## 1. Verify prerequisites

- [x] 1.1 Confirm `get_env` is available in Zola 0.22 templates (Open Question Q1). If unavailable, switch D3 to an env-derived value injected by the `serve` task and update design.md before proceeding.
- [x] 1.2 Confirm Tailwind `@theme` in `generated.css` declares each token default on `:root` (basis for removing partial fallbacks).

## 2. Token registry

- [x] 2.1 Create `data/tokens.toml` listing every token with `key`, `css_var`, `group` (`light`/`dark`/`font`).
- [x] 2.2 Load the registry via `load_data` in a shared spot reusable by `theme-vars.html` and `customizer.html`.

## 3. theme-vars partial (overrides only, no !important)

- [x] 3.1 Rewrite `templates/partials/theme-vars.html` to loop the registry and emit a `:root`/`.dark` line **only** for keys set in `config.extra.theme`, with no `!important`.
- [x] 3.2 Remove the hardcoded `{% else %}` default block and any literal default values.
- [x] 3.3 Ensure the `<style>` block is placed in `<head>` after the `generated.css` `<link>`; verify cascade order in page source.
- [x] 3.4 Keep font `<link>` emission for configured `font_*_link` values.

## 4. Dev gating via ZOLA_ENV

- [x] 4.1 Gate the `{% include "partials/customizer.html" %}` in `base.html` behind `get_env(name="ZOLA_ENV", default="") == "dev"`.
- [x] 4.2 Update `mise.toml` so `serve` exports `ZOLA_ENV=dev`; confirm `build` does not set it.
- [x] 4.3 Remove all references to `customizer_enabled` from templates.

## 5. Customizer rewrite

- [x] 5.1 Build the Alpine component from the registry (inputs generated by looping tokens), not hardcoded fields.
- [x] 5.2 On `init()`: load `sessionStorage` draft if present (apply to `:root` inline styles + populate inputs); else seed inputs from `getComputedStyle(documentElement)`.
- [x] 5.3 On input change: set the CSS variable inline on `documentElement` and write the full draft to `sessionStorage`.
- [x] 5.4 Add a Reset action that clears the `sessionStorage` draft and reverts to the configured/default cascade.
- [x] 5.5 Replace `getToml()` string concatenation with a registry-driven exporter covering `[extra.theme]` and `[extra.sections]`.

## 6. Section toggles macro

- [x] 6.1 Add a Tera `section(id, enabled, dev)` macro: prod omits disabled wrapper+content; dev always emits wrapper with `display:none` when disabled.
- [x] 6.2 Replace the copy-pasted ladders in `templates/index.html` with macro calls for `banner`, `features`, `testimonials`, `cta`.
- [x] 6.3 Wire the customizer's section checkboxes to the dev wrappers via `getElementById`.

## 7. Consuming site + docs

- [x] 7.1 In `worrydont-zola/zola.toml`, remove `customizer_enabled`; keep only intended `[extra.theme]` overrides.
- [x] 7.2 Rewrite the README customizer section: drop the manual-flag production warning, document `mise run serve` dev-gating and the Reset/sessionStorage behavior.
- [ ] 7.3 Bump the theme submodule pointer in `worrydont-zola`.

## 8. Verification

- [x] 8.1 `mise run build` output contains no customizer markup/JS and omits disabled sections.
- [x] 8.2 `mise run serve` renders the customizer; color/font edits apply live, persist across refresh, and Reset reverts.
- [x] 8.3 Export produces valid TOML covering all registry tokens + sections.
- [x] 8.4 `openspec validate redesign-white-label-theming --strict` passes.

## 9. Deferred (flagged, not in this change)

- [ ] 9.1 Decide whether to add the additional sections `hero`, `blog`, `footer`, `navigation` (register via the macro) — Open Question Q2.
- [ ] 9.2 Resolve the phantom `about` key: either add a real `about` section or drop it from any starter config.
